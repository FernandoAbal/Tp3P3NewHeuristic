package controller;

import java.util.ArrayList;

import model.NegocioSala;
import model.Solver;

public class Main {

	public static void main(String[] args) {
		new Controller();
		/*NegocioSala sala = new NegocioSala();
		sala.setearReservasDePrueba();
		sala.printReservas();
		
		ArrayList<String> horario = Solver.setearHorario();
		
		Solver.marcarHorarios(horario, sala.getReserva("Tesseract"));
		Solver.marcarHorarios(horario, sala.getReserva("Vola"));
		Solver.marcarHorarios(horario, sala.getReserva("Jinjer"));
		Solver.marcarHorarios(horario, sala.getReserva("Thy Art Is Murder"));
		Solver.marcarHorarios(horario, sala.getReserva("Bring Me The Horizon"));
		System.out.println(horario);
		*/
	}

}
